name: Deploy & Release

on:
  workflow_dispatch:
    inputs:
      with_d1:
        description: "Deploy D1"
        required: false
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write
  issues: write

# ジョブは D1 マイグレーション → Worker デプロイ → Release の順に直列化する。
# 並列だと「新しいコードが未適用スキーマに当たる」時間帯が生まれ、Release タグも
# マイグレーションの成否と無関係に打たれてしまうため。
#
# with_d1=false のとき db-deploy は skipped になる。後続ジョブの既定条件は
# success() で skipped も止めてしまうので、!failure() && !cancelled() を使って
# 「失敗・キャンセルのときだけ止める」に変えている。
jobs:
  db-deploy:
    runs-on: ubuntu-latest
    name: D1 Deploy
    if: ${{ inputs.with_d1 }}
    steps:
      - uses: actions/checkout@v5
      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: d1 migrations apply --remote DB

  build:
    runs-on: ubuntu-latest
    needs: db-deploy
    if: ${{ !failure() && !cancelled() }}

    steps:
      - uses: actions/checkout@v5
      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10
      - name: Use Node.js 22
        uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: "pnpm"
      - name: Install dependencies
        run: pnpm install

      - name: Deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          secrets: |
            FRONTEND_BASEURL
            ADMIN_ID
            ADMIN_PWD
        env:
          FRONTEND_BASEURL: ${{ secrets.FRONTEND_BASEURL }}
          ADMIN_ID: ${{ secrets.ADMIN_ID }}
          ADMIN_PWD: ${{ secrets.ADMIN_PWD }}

  release:
    runs-on: ubuntu-latest
    name: Release
    outputs:
      release_tag: ${{ steps.release_tag.outputs.VERSION }}
    needs: build
    if: ${{ !failure() && !cancelled() }}
    steps:
      - uses: actions/checkout@v5

      - name: Generate release tag
        id: release_tag
        run: |
          VERSION=$(date +'%Y.%m.%d.%H%M')
          echo "VERSION=v$VERSION" >> $GITHUB_OUTPUT
        env:
          TZ: "Asia/Tokyo"

      - name: Release
        run: |
          gh release create ${{ steps.release_tag.outputs.VERSION }} \
            --title "Release ${{ steps.release_tag.outputs.VERSION }}" \
            --generate-notes \
            --prerelease=false \
            --target main \
            --latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
